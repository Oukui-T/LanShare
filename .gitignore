# 1. 局部 Python 虚拟环境 (实际存在)
.venv/

# 2. 局部 Agent 与 Skill 技能包 (实际存在)
.agents/

# 3. Nuitka 编译封装产物与绿色运行包 (实际存在及构建产生)
dist/
*.dist/
*.build/
*.onefile-build/

# 4. 自动化测试套件与测试数据目录 (实际存在)
tests/

# 5. Python 运行期缓存与编译字节码 (实际产生)
__pycache__/
*.pyc
.pytest_cache/

# 6. 文档过滤：忽略 docs 下的开发规格与内部设计文档，仅保留用户使用手册目录 (docs/manual)
docs/*
!docs/manual/
!docs/manual/**

# 7. 忽略项目打包与编译脚本（不对外开源编译工具链）
build_client.py
build_server.py
